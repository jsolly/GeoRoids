---
extends: default

ignore: |
  node_modules/
  dist/
  .astro/
  .aws-sam/
  .vercel/
  .claude/
  .venv/
  .playwright-cli/
  .playwright-mcp/
  .tmp/
  .npm-cache/

rules:
  # Default warning rules must be errors even without the CLI --strict flag.
  comments:
    level: error
  comments-indentation:
    level: error
  document-start:
    level: error
  truthy:
    level: error
  # 80-char max is too tight for YAML carrying URLs, ARNs, and inline scripts.
  line-length: disable
